# =========================
# LMS - Server .env (example)
# =========================
# Copy this to the server as ".env" (NOT committed), then edit the values.

# ---- App ----
SECRET_KEY=change-me-32-characters-minimum
LOG_LEVEL=INFO
LOG_JSON=1

# ---- Choose ONE database configuration ----
# A) Containerized Postgres on the same host (compose "db" service)
#    The app will use host=db on the compose network.
DB_HOST=db
DB_PORT=5432
DB_NAME=lms_db
DB_USER=lms_user
DB_PASSWORD=please-change
# Optional single URL (compose will also pass DB_*; app can build URL if needed)
# DATABASE_URL_DOCKER=postgresql+psycopg2://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# B) Amazon RDS / External Postgres (uncomment and set)
# DATABASE_URL=postgresql+psycopg2://<user>:<password>@<rds-endpoint>:5432/<dbname>

# ---- Gunicorn ----
GUNICORN_WORKERS=4
GUNICORN_TIMEOUT=60
