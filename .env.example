# Flask
FLASK_ENV=development
FLASK_DEBUG=1

# Database (local dev with Docker Compose)
DATABASE_URL=postgresql+psycopg2://lms_user:lms_pass@localhost:5432/lms_db

# Flask / App
SECRET_KEY=change-me-in-prod
LOG_LEVEL=INFO
LOG_JSON=0  # 1 to emit JSON logs

# Gunicorn (containerized runs)
GUNICORN_WORKERS=2
GUNICORN_TIMEOUT=60

# Database (placeholders; not real secrets)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=lms_db
DB_USER=lms_user
DB_PASSWORD=change-me

# For the app (dotenv usually resolves ${...}; if not, keep the full URL in your real .env)
DATABASE_URL=postgresql+psycopg2://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}
